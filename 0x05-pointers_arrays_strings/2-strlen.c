#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - check the code
 * @s: string
 * Return: Always 0.
*/
int _strlen(char *s)
{
	int lon = 0;

	while (*S != '\0')
	{
		lon++;
		*S++;
	}
	return (lon);
}
